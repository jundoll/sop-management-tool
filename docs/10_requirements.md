# 実装仕様書：AI連携型 ステップ形式SOPシステム

## 1. 目的・背景

### 1.1 システムの目的
定型業務の標準化、ヒューマンエラー防止、およびAIによる手順作成・改定の効率化を図る。

### 1.2 解決すべき課題
- 既存手順書における「表組みによるAI読み取りの阻害」
- 人力修正による負荷とミス
- 手順書の作成・改定作業の非効率性

### 1.3 期待する効果
- AIが読み取りやすい構造化されたフォーマットでのSOP管理
- システム的な「自動生成感」を排し、Excelで手作成したような標準的で可読性の高いビジネス文書を出力
- 作業者の実施手順の確実な遂行と記録の自動化

### 1.4 対象ユーザー
| 役割 | 説明 |
|------|------|
| SOP作成者 | 管理者画面を使用してSOPの作成・編集・レビュー依頼を行う |
| レビュー者 | 作成されたSOPを確認・承認する |
| 作業者（実施者） | 実施画面を使用してSOPに沿った作業を遂行し、実績を記録する |
| チェッカー | 作業者の実施をリアルタイムで目視確認する |

---

## 2. システム構成・環境

### 2.1 基盤
- **任意のWebホスティング環境（SharePoint含む）**
- ファイルシステム上で単体HTMLとして完結
- npm等のビルド環境を必要としない

### 2.2 開発形態
- **Webアプリケーション (Frontend-only)**
- Vanilla HTML/CSS/JavaScriptで構成
- npm実行環境に依存しない

### 2.3 データ保持
| 種別 | 形式 | 保存先 |
|------|------|-------|
| テンプレート | JSON形式 | ブラウザのlocalStorage |
| 実績データ | メモリ上管理 | エクスポート時のみExcel(.xlsx)生成 |

### 2.4 ブラウザ要件
- **Microsoft Edge (最新版)** を推奨
- モバイルブラウザは対象外

### 2.5 配布・ポータビリティ
- 単一フォルダ内のHTML/CSS/JSファイルで構成
- 配布時はnpm関連ファイル（package.json, tsconfig.json, node_modules等）を削除
- 他プロジェクトへファイルを渡すだけで同じシステムを再利用可能

---

## 3. データ構造 (JSONスキーマ定義)

```json
{
  "sop_id": "string",
  "sop_title": "string",
  "steps": [
    {
      "step_index": "integer",
      "instruction": "string",
      "media_enabled": "boolean",
      "require_time": "boolean",
      "require_judgment": "boolean",
      "skip_enabled": "boolean",
      "images": ["base64"],
      "comment": "string"
    }
  ]
}
```

### フィールド説明
| フィールド | 型 | 説明 |
|-----------|------|------|
| `sop_id` | string | SOPを一意に識別するID |
| `sop_title` | string | SOPのタイトル |
| `steps[].step_index` | integer | ステップの順序（1始まり） |
| `steps[].instruction` | string | ステップの作業指示内容 |
| `steps[].media_enabled` | boolean | 画像添付が有効かどうか |
| `steps[].require_time` | boolean | 時刻記録が必要かどうか |
| `steps[].require_judgment` | boolean | OK/NG判定が必要かどうか |
| `steps[].skip_enabled` | boolean | スキップが許可されているかどうか |
| `steps[].images` | array[string] | 参考画像（Base64）の配列 |
| `steps[].comment` | string | 作業者向けの追加コメント |

---

## 4. 画面遷移定義

### 4.1 画面構成

| 画面ID | 画面名 | 説明 | 対応画面ファイル |
|--------|-------|------|----------------|
| SC-01 | TOP画面（SOP一覧） | SOPの一覧表示、新規作成、インポート、編集・実施の選択 | `js/app.js` (showSelectionView) |
| SC-02 | 作成画面（Admin View） | SOPの作成・編集・ステップ管理・プレビュー | `js/admin.js` (showAdminView) |
| SC-03 | 実施画面（Player View） | SOPに沿った作業の実行・記録（全ステップ一覧形式、スクロール表示） | `js/player.js` (renderSteps) |
| SC-04 | 実施開始画面 | 実施開始確認（作業者名入力なし） | `js/player.js` (showStartScreen) |
| SC-05 | プレビュー（モーダル） | 編集中SOPの全ステップ確認（オーバーレイ表示） | `js/admin.js` (openPreview) |
| SC-06 | 未保存確認ダイアログ（モーダル） | 未保存状態での画面離脱時の確認 | `js/app.js` (confirmUnsavedChanges) |

### 4.2 画面遷移一覧

| 遷移元 | 遷移先 | トリガー | 備考 |
|-------|-------|---------|------|
| SC-01 TOP画面 | SC-02 作成画面 | 「+ 新規SOP作成」ボタン押下 | 空のSOPを生成して作成画面を開く |
| SC-01 TOP画面 | SC-02 作成画面 | SOPカードの「編集」ボタン押下 | 選択したSOPを読み込んで作成画面を開く |
| SC-01 TOP画面 | SC-04 実施開始画面 | SOPカードの「実施」ボタン押下 | 選択したSOPの実施開始画面へ遷移（作業者名は不要） |
| SC-01 TOP画面 | SC-01 TOP画面 | 「JSONファイルから読み込み」ボタン押下 → ファイル選択 → 読み込み完了 | インポート成功後、一覧を再表示 |
| SC-01 TOP画面 | —（ファイルダウンロード） | SOPカードの「エクスポート」ボタン押下 | JSONファイルがダウンロードされる（画面遷移なし） |
| SC-01 TOP画面 | SC-01 TOP画面 | SOPカードの「削除」ボタン押下 | 確認後、一覧から削除して再表示 |
| SC-02 作成画面 | SC-01 TOP画面 | 「選択画面へ」ボタン押下 | 未保存時はSC-06確認ダイアログを表示 |
| SC-02 作成画面 | SC-05 プレビュー（モーダル） | 「プレビュー」ボタン押下 | 編集中SOPの全ステップをオーバーレイ表示 |
| SC-02 作成画面 | SC-02 作成画面 | 「保存」ボタン押下 | localStorageに保存後、画面はそのまま |
| SC-04 実施開始画面 | SC-03 実施画面 | 「開始」ボタン押下 | 第1ステップから全ステップを一覧表示 |
| SC-03 実施画面 | SC-03 実施画面 | OK/NG/スキップボタン押下 | 各ステップごとに判定・時刻記録、全ステップ完了後完了画面へ |
| SC-03 実施画面 | SC-01 TOP画面 | 最終ステップ完了 | 実績データ保持、TOP画面へ戻る |
| SC-06 未保存確認ダイアログ | SC-01 TOP画面 | 「保存して戻る」選択 | SOPを保存後、TOP画面へ遷移 |
| SC-06 未保存確認ダイアログ | SC-01 TOP画面 | 「破棄して戻る」選択 | 保存せずTOP画面へ遷移 |
| SC-06 未保存確認ダイアログ | SC-02 作成画面 | 「キャンセル」選択 | ダイアログを閉じて編集画面に留まる |

### 4.3 メニューバー遷移

全画面共通のメニューバーから以下の遷移が可能。メニュー押下時は常に現在の画面状態をチェックし、未保存状態の場合はSC-06確認ダイアログを経由する。

| メニュー項目 | 遷移先 | 動作 |
|------------|-------|------|
| TOP | SC-01 TOP画面 | SOP一覧画面へ遷移 |
| SOP作成 | SC-02 作成画面 | 編集中のSOPがなければ新規作成、あればそのまま作成画面へ |
| 実施 | SC-03 実施画面 | 編集中SOPがあればそのSOPの実施画面、なければSOP選択用の一覧を表示 |

### 4.4 状態復元（リロード耐性）

| 保存状態 | 復元動作 |
|---------|---------|
| 作成画面で編集中だった | 前回編集中のSOPを復元し作成画面を再表示 |
| 実施画面で作業中だった | 前回のステップ位置と実績データを復元し実施画面を再表示 |
| それ以外 / データなし | TOP画面（SOP一覧）を表示 |

---

## 5. 機能要件詳細

### 5.1 TOP画面（SOP一覧画面）

#### 5.1.1 SOP一覧表示
- 登録済みSOPの一覧を表示
- 各SOPを選択してSOP作成画面または実施画面へ遷移
- 新規SOP作成ボタンを提供
- **新規作成ボタンとJSON読み込みボタンはコンパクトなレイアウトにする**
  - ボタンの縦方向の専有面積を抑え、横並びまたは最小限の高さで配置する
  - 過剰なパディングやフォントサイズを避け、標準的なボタンサイズとする
- **「SOP管理システム」のカードヘッダーエリアと「SOP一覧」エリアは1つのカードに統合する**
  - 画面上部のヘッダー的エリアをなくし、操作ボタンと一覧を同一カード内に収める
  - 「SOP管理システム」というタイトルはメニューバーのブランド表示に委ねる

#### 5.1.2 JSONインポート
- JSONファイルからSOPをインポートするUIを提供
- インポート時は既存SOPを上書きせず、**新規SOPとして追加**する
- インポート後はSOP一覧を即時更新して反映する

#### 5.1.3 画面状態の保持（リロード耐性）
- ブラウザリロード時にTOP画面へ強制遷移しない
- リロード前の画面状態（編集中のSOP・実施中のSOP等）をlocalStorageに保存し、復元可能とする

### 5.2 作成画面 (Admin View)

#### 5.2.1 SOP管理
- 複数SOPの作成・一覧・選択
- SOPの保存：localStorageに自動保存
- SOPの読み込み：localStorageから自動復元
- JSONファイルからのインポート時は、SOP一覧を即時更新して反映する

#### 5.2.2 ステップ管理（CRUD）
- ステップの追加、削除
- ドラッグ＆ドロップでの並び替え
- **ステップ追加は1回の操作で1ステップのみ追加する（複数ステップが追加されるバグを修正）**
- **既存ステップ編集後の保存操作では、新規ステップが複数追加されないようにする**

#### 5.2.3 各ステップ設定項目
- テキスト入力欄（作業指示内容の編集）
  - 広めの編集エリアを提供
  - **編集ウィンドウ/モーダルを開かずにインラインで直接編集可能とする**
- 参考画像貼り付け（複数枚）
  - クリップボードからのペースト（貼り付け）のみ対応
  - 画像は十分なサイズのエリアに表示し、**拡大プレビュー（クリックで拡大）**と削除が可能とする
- **各ステップの詳細表示で、作業指示とコメントを縦に並べ、その横に画像を配置するレイアウトとする**
- **画像の貼り付けが確実に動作するようにする（モーダル内の画像アップロードエリアを含む）**
- **詳細を開いた後に、開閉ボタン（▼/▲）と削除ボタンの配置を見直し、動線に沿って使いやすく配置する**
- 画像プレースホルダー表示（常時ON、設定変更不可）
- 以下の制御は全ステップ共通で常時ON（設定UIは表示しない）
  - 時刻記録必須
  - OK/NG判定必須
  - スキップ許可

#### 5.2.4 未保存時の確認ダイアログ
- TOP画面から新規SOP作成後、保存せずに選択画面/一覧へ戻ろうとした場合、確認ダイアログを表示する
- ダイアログの選択肢：
  - **保存して戻る**：現在のSOPを保存して一覧へ戻る
  - **破棄して戻る**：保存せずに一覧へ戻る（未保存SOPは破棄）
  - **キャンセル**：編集画面に留まる
- 未保存の0ステップSOPが自動的に追加されないようにする

#### 5.2.5 プレビュー機能
- 選択中のSOPの全ステップを確認できるSOP全体プレビューを表示
- 実施画面（Player View）への遷移は行わない
- **プレビュー表示中でもヘッダーの保存ボタンが機能するようにする（プレビュー状態での保存操作を可能とする）**
- **プレビューボタンを押さなくても、作成画面で全ステップの内容を直接閲覧・編集できるようにする**
  - ステップ一覧に各ステップの指示内容・コメント・画像サムネイルをインライン表示する
  - ステップを展開/折りたたみ可能にし、クリックで詳細を表示する
  - プレビューボタンは補助的な全画面表示として残す

#### 5.2.6 画面名称
- **「管理者画面」ではなく「作成画面」という名称を使用する**
  - メニューバーの表示: 「SOP作成」
  - 画面上のタイトル表示: 「作成画面」
  - コード内のコメント・変数名等は変更しない（表示名のみ）

### 5.3 実施画面 (Player View)

#### 5.3.1 画面レイアウト
- **ウィザード形式（1ステップずつ）から全ステップを一覧表示するスクロール形式に変更する**
  - 全ステップを縦に並べてスクロールで確認可能にする
  - 各ステップは1行にまとめて表示し、ユーザーが順次作業を進められるようにする
- **各ステップで、左側に作業内容・補足コメントを、右側にエビデンス画像を並べるレイアウトとする**
  - 画面を左右2カラムに分割する
  - 左カラム: 指示内容、補足コメント、作業コメント入力、スキップ理由入力
  - 右カラム: 参照画像、エビデンス画像貼り付けエリア

#### 5.3.2 ステップ遷移
- 全ステップを1画面に表示するため、**「次へ」ボタンは不要**
- 各ステップのOK/NG/スキップ判定はその場で行い、完了時刻は自動記録する
- 作業完了後は**TOP画面（SOP一覧画面）へ戻る**

#### 5.3.3 入力項目
| 項目 | 入力方式 | 説明 |
|------|----------|------|
| 参照画像 | 画面表示（右カラム） | 管理者が貼り付けた参考画像を表示。**クリックで拡大表示** |
| コメント | 画面表示（左カラム） | 管理者が入力した補足コメントを表示 |
| 完了時刻 | OK/NG/スキップ押下時に自動記録 | 現在時刻(HH:mm:ss)を自動取得 |
| OK/NG判定 | トグル形式のボタン選択 | 2択選択。押下で時刻記録 |
| 画像添付 | Ctrl+V (ペースト)（右カラム） | クリップボード画像を取得し、複数枚表示。貼り付けエリアは十分なサイズを確保。**貼り付け欄に「Ctrl+Vで貼り付け」のプレースホルダーを表示**。**「ペースト」ボタンは非表示** |
| 作業コメント | テキスト入力（左カラム） | 実施記録として自由入力 |

#### 5.3.4 画像貼り付け・管理（実施画面）
- **「作業画像」横の「ペースト」ボタンを削除する**
- 貼り付け欄に「Ctrl+Vで貼り付け」のプレースホルダーテキストを表示
- 貼り付け欄のサイズを十分に大きくする
- 貼り付けた画像には削除用のバツ（×）ボタンを表示し、**押下で画像が削除されるようにする**
- 手順に貼り付けられた画像をクリックしたら拡大表示する

#### 5.3.5 スキップ機能
- 各ステップにあらかじめ**スキップ理由入力欄**を用意する（スキップボタン押下時にウィンドウ表示ではなく）
- スキップ理由を入力した上でスキップボタンを押下するとスキップされる
- スキップ時も完了時刻が自動記録される

#### 5.3.6 例外処理
| 状況 | 動作 |
|------|------|
| スキップボタン押下 | あらかじめ用意されたスキップ理由入力欄に入力後、スキップ実行 |
| 未入力状態でスキップボタン押下 | スキップ理由入力欄の近くに赤字で「スキップ理由を入力してください」とメッセージを表示する（モーダルウィンドウは使用しない）。入力を促し、スキップは実行しない |
| 完了後 | TOP画面（SOP一覧画面）へ遷移 |

#### 5.3.7 作業者名
- **実施開始画面および実施画面に作業者名の入力欄は不要とする**
- 実施開始画面は「開始」ボタンのみのシンプルな構成とする

#### 5.3.8 ネットワーク挙動
- **常時オンライン前提**（SharePoint等での配布時）
- 通信断絶時は `alert()` にて警告
- データはブラウザ内（localStorage/メモリ）で保持

---

## 6. 非機能要件

### 6.1 パフォーマンス
- 画面遷移時の応答時間：操作から次の画面表示まで **2秒以内**
- Excel生成時間：最大30ステップのSOPで **5秒以内**
- 画像ペーストからプレビュー表示まで **1秒以内**

### 6.2 セキュリティ
- **クリップボードAPI制約**: `navigator.clipboard.read()` を使用するため **HTTPS配信必須**
- データはブラウザのローカルストレージ（localStorage）とメモリ上で保持
- 画像データはブラウザのメモリ上でのみ保持し、ローカルファイルシステムへの保存は行わない（エクスポート時を除く）

### 6.3 ブラウザ互換性
- **Microsoft Edge (最新版)** を推奨
- モバイルブラウザは対象外

### 6.4 データ永続性
- テンプレートデータ：localStorageへ自動保存
- ブラウザのキャッシュクリア等によりデータが消失する可能性あり
- 実績データはエクスポートでのみ外部保存

### 6.5 アクセシビリティ
- キーボード操作による全機能の利用を可能とする
- スクリーンリーダー対応については将来的な検討課題とする

### 6.6 UI/UXデザイン要件
- 全画面において**美しく洗練されたデザイン**を適用する
- 色彩、フォント、余白、アニメーション等はモダンなWebアプリケーションに準じた品質とする
- 一貫性のあるデザインシステム（カラーパレット、タイポグラフィ、コンポーネントスタイル）を整備する
- **全ページの上部に統一されたメニューバー（ナビゲーションバー）を1つだけ配置する**
  - メニューバーには「TOP」「SOP作成」「実施」の3つの主要画面へのリンク/ボタンを配置
  - 全画面で同一のデザイン・配置とし、統一感を確保する
  - 現在の画面が視覚的に識別できるアクティブ表示を行う
  - **メニューバーは1つに統一し、重複したバー（app-header等）は表示しない**
  - **メニューバー内の不要なトグルボタン（管理画面/実施画面）は削除する**
- **メニューバーのアクティブ状態と実際の表示画面を常に一致させる**
  - メニュー押下時の画面遷移でアクティブ表示が正しく更新されることを保証する
  - 画面復元（リロード）時もアクティブ表示が正しい画面と一致する
- **画面遷移関係を明確に定義する（4.2節参照）**
  - 各画面間の遷移トリガーと動作を一覧化する
  - 未保存状態での遷移は確認ダイアログを経由する
  - メニューバーからの遷移も含めて一貫性を確保する
- **メニューバーのブランド部分（「SOP管理システム」）をクリックするとTOP画面（SOP一覧画面）へ遷移する**
  - ブランドテキストをリンク/ボタンとして機能させ、クリックで `showSelectionView()` を呼び出す
  - 遷移時は未保存チェック（`confirmUnsavedChanges`）を経由する

---

## 7. 出力成果物要件 (Excel Export)

### 7.1 フォーマット
- **.xlsx (OpenXML形式)**

### 7.2 レイアウト定義
- シート1枚に全工程を縦に並べる
- **列構成**: [No, 作業内容, 実施時刻, 判定, スキップ理由, コメント, 画像]
- **フォント**: 游ゴシック 11pt
- **罫線**: 標準的な細実線
- **画像配置**: 画像列には複数枚の画像を1列に縦に並べて埋め込み（セル結合で複数行にまたがる形）

### 7.3 画像最適化
- 画像添付時にJavaScriptにて以下を実施
  - **最大幅1280px** へのリサイズ
  - **JPEG品質係数0.8** での圧縮（または同等の圧縮率）
- ファイルサイズが大きくなりすぎないよう制御

---

## 8. 運用・ワークフロー

### 8.1 作成・実施フロー
1. 管理者が作成画面でSOPを新規作成
2. 各SOPにステップを定義し、設定を編集
3. 作成者がTeams等でファイルを共有
4. 作業者がSOPを開き、ステップごとに作業を実行・記録
5. 全ステップ完了後、実績データをExcel出力

### 8.2 データ共有方法
- テンプレートデータはlocalStorageに保存（同一ブラウザ内で永続化）
- 実績データはExcelファイルとしてエクスポートして共有
- 別PCへのデータ移行はエクスポート/インポート機能で対応
- SOP一覧画面(選択画面)にJSONファイルからインポートするUIを提供する

### 8.3 権限管理
- ファイルシステムまたはSharePointの標準権限に依存
- 特殊なシステム内権限制御は実装しない

---

## 9. ユースケース

### 9.1 基本シナリオ
1. **SOP作成**: 作成者がAdmin Viewで新規SOPを作成し、ステップを定義する
2. **SOP保存**: localStorageに自動保存され、ブラウザを閉じてもデータが保持される
3. **SOP実施**: 作業者がPlayer ViewでSOPを開き、ステップごとに作業を実行・記録
4. **実績出力**: 全ステップ完了後、実績データをExcelファイルとしてエクスポート

### 9.2 例外シナリオ
| シナリオ | 期待動作 |
|----------|----------|
| ネットワーク断発生 | alert()で警告（データはlocalStorageに保持） |
| 画像ペースト失敗 | エラーメッセージを表示し、再ペーストを促す |
| ブラウザのキャッシュクリア | localStorageのデータが消失（運用でバックアップ推奨） |
| 実施途中でブラウザ閉じる | localStorageに保存済みデータは維持される（次回開時に復元） |

---

## 10. ドキュメント要件

### 10.1 README構成
- 1つのREADME.mdに以下を含める:
  1. 概要（システムの目的と特徴）
  2. セットアップ方法（npm不要、HTMLファイルをブラウザで開くだけ）
  3. 使い方（管理者・作業者向けの操作説明）
  4. カスタマイズ方法（機能拡張やブラウザ要件）

### 10.2 技術リスク
| リスク | 影響 | 対策案 |
|--------|------|--------|
| クリップボードAPIのブラウザ互換性 | Edge以外での動作未保証 | 対象ブラウザをEdgeに限定する方針で合意済み |
| HTTPS未配信環境でのクリップボードAPI非対応 | 画像ペースト機能が動作しない | 開発・検証環境もHTTPSで統一 |
| Excel生成ライブラリのフロントエンド制約 | 大規模データでのメモリ不足 | ライブラリ選定時にメモリ使用量を評価 |
| localStorageの容量制限 | 大量の画像データを保存できない | 画像のリサイズ・圧縮で容量削減 |

### 10.3 選定が必要な技術要素
- **Excel生成ライブラリ**: SheetJS (xlsx) CDN版
- **画像圧縮処理**: Canvas APIを使用したリサイズ・圧縮
- **UIフレームワーク**: Vanilla HTML/CSS/JavaScript (React/Vue.js不使用)

---

## 11. 変更履歴

| 日付 | バージョン | 変更内容 | 担当 |
|------|-----------|---------|------|
| 2025-01-11 | 1.0 | 初版作成 | - |
| 2025-01-11 | 1.1 | フィードバック反映: README構成の標準化、npm不要、ポータビリティ向上 | - |
| 2025-01-11 | 1.2 | フィードバック反映: 編集欄拡充、画像ペーストのみ対応、保存/複製バグ修正、プレビュー全体化、エクスポート機能追加、JSONインポートUI追加、P-01作業者名入力画面追加 | - |
| 2025-01-11 | 1.3 | フィードバック反映: 作業編集欄のレイアウト改善、重複登録バグ修正、保存機能を確実に動作させる、SOPテンプレートのエクスポート(JSON)機能追加 | - |
| 2025-01-11 | 1.4 | フィードバック反映: JSONインポート後のSOP一覧即時更新、完了時刻/OKNG判定の誤判定修正、キャンセルで遷移キャンセル、画像エリア拡張・拡大プレビュー・削除 | - |
| 2025-01-11 | 1.5 | フィードバック反映: JSONインポート既存上書き→新規追加、ステップ重複追加バグ修正、未保存時確認ダイアログ、インライン編集、画像クリック拡大、ペーストボタン削除/Ctrl+V表示、貼付欄拡大、画像削除バグ修正、OK/NG/スキップ自動次へ＋時刻記録、スキップ理由欄事前配置、完了後TOP画面遷移、リロード耐性、美しいデザイン | - |
| 2026-07-28 | 1.6 | フィードバック反映: プレビュー状態での保存操作対応、スキップ未入力時のモーダル→赤字インラインメッセージに変更 | - |
| 2026-07-28 | 1.7 | フィードバック反映: JSONインポート新規追加（上書き防止）、プレビュー保存修正、Ctrl+V表示、スキップ赤字メッセージ、ペーストボタン削除、美しいデザイン、デザイン統一、全ページメニューバー追加 | - |
| 2026-07-29 | 1.8 | フィードバック反映: メニューバー統一（重複バー削除・不要トグル削除）、メニューactive状態と画面の一致保証、画面遷移関係の明確化 | - |
| 2026-07-29 | 1.9 | フィードバック反映: 作成画面でプレビュー相当の内容を直接閲覧可能に（ステップ展開表示）、「管理者画面」→「作成画面」に名称変更 | - |
| 2026-07-29 | 1.10 | フィードバック反映: TOP画面の新規作成/JSON読込ボタンの専有面積をコンパクト化 | - |
| 2026-07-29 | 1.11 | フィードバック反映: メニューブランド「SOP管理システム」クリックでTOP遷移 | - |
| 2026-07-29 | 1.12 | フィードバック反映: SOP一覧画面のカード統合、SOP実施画面の全ステップ一覧スクロール形式＋左右レイアウト変更、作業者名削除、作成画面の詳細レイアウト改善、画像貼り付けバグ修正 | - |
| 2026-07-29 | 1.13 | フィードバック反映: フィードバック履歴管理不要、全画面デザイン統一、レイアウト統一、SOP一覧/ステップ一覧/実施作業一覧のレイアウト統一検討、SharePoint前提、既存ライブラリ活用、ユースケース定義、実態に沿った名称。SOP一覧の新規作成/JSON読込ボタンをコンパクト化、SOP管理システムと一覧を統合。作成画面でインライン編集・詳細展開、作業とコメント縦並び＋画像横並び、画像貼り付け修正、クリックで開閉、ボタン配置見直し。実施画面の左右レイアウト維持、スクロール形式確認、作業者名削除 | - |
